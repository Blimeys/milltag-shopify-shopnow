<div class="container offset-80">
  <div class="on-duty-box">
    <img src="{{ "page-404-icon.png" | asset_url }}" alt="{{ "general.404.title" | t }}">
    <h1 class="block-title large">{{ "general.404.title" | t }}</h1>
    <div class="description">{{ "general.404.subtext" | t }}</div>
    <a class="btn btn-border color-default" href="/">{{ "general.404.button" | t }}</a>
  </div>
</div>